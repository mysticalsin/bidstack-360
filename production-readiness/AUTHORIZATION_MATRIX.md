# BidStack 360° — Authorization Matrix (Gate 4)

**Date:** 2026-06-28 · **Branch:** feat/prod-hardening-mantu · **Method:** static review of `apps/api/src/plugins/rbac.ts` + `auth.ts` and every `post/put/patch/delete` handler across `apps/api/src/routes/**` (313 mutation routes), cross-referenced against per-route `preHandler` and file-level `server.addHook('preHandler', requirePermission(...))` hooks, then manually verified each flagged route.

**Result:** 313 mutation routes reviewed. The previously listed Gate 4 AI-compute authorization gaps are now fixed in current WIP: AI assistant compute is human-session only plus route-specific read permissions, and call insight re-analysis is human-session only plus `activities:write`. Remaining authenticated-only rows in the matrix are owner/self-scoped or intentionally product-scoped follow-ups, not known open AI-compute gaps.

---

## 1. How auth/authz works here (critical context)

- **Global authentication:** `auth.ts` `onRequest` hook authenticates EVERY request (Clerk JWT / API key / dev stub) unless the route sets `config:{ public:true }`. A mutation with no `preHandler` is therefore still **authenticated** — the risk is **broken authorization** (a read-only-role user, or a read-scoped API key, can mutate), not unauthenticated IDOR.
- **`requirePermission(key)`** (`rbac.ts`): for human users, checks a `UserRole → RolePermission` grant for the exact permission key — **no admin-claim fallback; the DB authorizes**. For REST API keys (`role === 'api'`), production checks the exact permission scope (for example `integrations:write`). Legacy broad `read`/`write` fallback is enabled only outside production by default, or by explicit `BIDSTACK_ALLOW_LEGACY_API_KEY_SCOPES=true` during migration.
- **`requireRole(...names)`** (`rbac.ts`): always 403s API keys; otherwise checks the named role via `UserRole`. (Hardened this session line of work — see [SECURITY_REVIEW](./SECURITY_REVIEW.md): API-role principals are now explicitly rejected and role lookup is DB-authoritative + case-insensitive.)
- **Consequence:** a route with NO permission/role gate (per-route or file hook) has ZERO permission check, so any authenticated principal, including an API key that passes global auth, can invoke it.

---

## 2. Resource × Action matrix (major resources)

Legend: `auth` = authenticated only (no permission gate) · `+admin` = also `requireRole('admin')`.

| Resource                                                   | Read                 | Create                                                                          | Update                           | Delete                                           | Source                                              |
| ---------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------- | -------------------------------- | ------------------------------------------------ | --------------------------------------------------- |
| Accounts                                                   | accounts:read        | accounts:write                                                                  | accounts:write                   | settings:write +admin                            | accounts.ts                                         |
| Account-intel (docs/solutions/products)                    | accounts:read        | accounts:write                                                                  | accounts:write                   | accounts:write                                   | account-intel.ts (file hook)                        |
| Contacts                                                   | auth                 | contacts:write                                                                  | contacts:write                   | contacts:write                                   | contacts.ts                                         |
| Companies (settings)                                       | companies:read       | companies:write +admin                                                          | +admin                           | +admin                                           | companies.ts                                        |
| Companies (CRM)                                            | auth                 | companies:write                                                                 | companies:write                  | companies:write                                  | crm/companies.ts                                    |
| Opportunities                                              | opportunities:read   | opportunities:write                                                             | opportunities:write              | opportunities:write                              | opportunities.\*.ts (file hook)                     |
| Leads                                                      | leads:read           | leads:write                                                                     | leads:write                      | leads:write                                      | leads.write.routes.ts (file hook)                   |
| Proposals                                                  | proposals:read       | proposals:write                                                                 | proposals:write                  | proposals:write (+admin op)                      | proposals.ts                                        |
| Tasks                                                      | auth                 | tasks:write                                                                     | tasks:write                      | tasks:write                                      | tasks.ts                                            |
| References                                                 | auth                 | accounts:write                                                                  | accounts:write                   | accounts:write                                   | references.ts (file hook)                           |
| Cross-sell                                                 | accounts:read        | accounts:write +human-actor                                                     | +human                           | +human                                           | cross-sell.ts                                       |
| Org-settings                                               | settings:read        | settings:write +admin                                                           | +admin                           | +admin                                           | org-settings.ts                                     |
| Users                                                      | users:read +admin    | users:write +admin                                                              | +admin                           | +admin                                           | users.ts                                            |
| Roles                                                      | settings:read +admin | settings:write +admin                                                           | +admin                           | +admin                                           | roles.ts                                            |
| User-groups                                                | settings:read        | settings:write +admin                                                           | +admin                           | +admin                                           | user-groups.ts                                      |
| API-keys                                                   | integrations:read    | integrations:write                                                              | —                                | integrations:write                               | dust-integration.ts                                 |
| Custom-fields                                              | customFields:read    | customFields:write +admin                                                       | +admin                           | +admin                                           | custom-fields.ts                                    |
| Custom-objects (defs)                                      | customObjects:read   | customObjects:write +admin                                                      | +admin                           | +admin                                           | custom-objects.defs.routes.ts                       |
| Custom-objects (records)                                   | customObjects:read   | customObjects:write                                                             | customObjects:write              | customObjects:write                              | custom-objects.records.routes.ts                    |
| Webhook-subscriptions                                      | webhooks:read        | webhooks:write +human                                                           | +human                           | +human                                           | webhook-subscriptions.ts                            |
| Webhook receivers (dust/docusign/twilio/graph/slack/calls) | —                    | **public** (signature/token verified in-handler)                                | —                                | —                                                | webhooks.ts, integrations/\*                        |
| Integrations: Dust                                         | integrations:read    | integrations:write                                                              | integrations:write               | integrations:write                               | dust-integration.ts                                 |
| Integrations: provider creds                               | integrations:read    | +admin                                                                          | +admin                           | +admin                                           | \*-credentials.routes.ts                            |
| Integrations: Gmail/MS/Slack OAuth                         | auth                 | connect/callback **public** (OAuth state CSRF)                                  | —                                | disconnect = auth, self-scoped                   | gmail.ts / microsoft-mail.ts / slack.ts             |
| **Email send**                                             | auth                 | **integrations:write** ✅ _(fixed this session)_                                | —                                | —                                                | integrations/email.ts:18                            |
| **SMS send**                                               | auth                 | **integrations:write** ✅ _(fixed)_ · test = **integrations:read** ✅ _(fixed)_ | —                                | —                                                | integrations/twilio.ts:205 / :179                   |
| **CRM widgets (org dashboard)**                            | auth                 | —                                                                               | **settings:write** ✅ _(fixed)_  | —                                                | crm/widgets.ts:18                                   |
| **AI assistant compute**                                   | —                    | **human + route read permission**                                               | feedback = human + session owner | —                                                | ai-assistant.ts:71/89/110/128/146/164               |
| **Calls (read/detail)**                                    | activities:read      | —                                                                               | —                                | —                                                | calls.read.routes.ts:25/115                         |
| **Calls extract-insights (AI re-run)**                     | —                    | **activities:write +human**                                                     | —                                | —                                                | calls.read.routes.ts:198                            |
| Calendar / Booking pages                                   | auth                 | auth, owner-scoped                                                              | owner-scoped                     | owner-scoped                                     | calendar.ts / bookings-pages.ts                     |
| Saved views / Notifications / Comments / Edit-locks        | auth                 | self/author-scoped                                                              | owner-or-admin                   | owner-or-admin                                   | saved-views/notifications/collaboration/realtime.ts |
| Tags / Territories / Bid-scores / Workflows                | \*:read              | \*:write                                                                        | \*:write                         | \*:write                                         | file hooks                                          |
| Analytics dashboards/reports                               | reports:read         | reports:write                                                                   | reports:write                    | reports:write                                    | analytics-\*.ts                                     |
| Audit-logs                                                 | audit-log:read       | —                                                                               | —                                | —                                                | audit-logs.ts                                       |
| KAM (all sub-resources)                                    | kam:read             | kam:write                                                                       | kam:write                        | kam:write                                        | kam-\*.ts                                           |
| Crews / Crew-agents / Monitoring                           | auth                 | +admin                                                                          | +admin                           | +admin (run=member, cancel/retry=owner-or-admin) | crews.ts                                            |
| Erasure / Tenant-export                                    | settings:write       | settings:write                                                                  | —                                | settings:write                                   | erasure.ts / tenant-export.ts                       |

---

## 3. Gaps fixed this session (commit: authZ hardening)

| Endpoint                         | Was       | Now                                       | Why                                                                                                                        |
| -------------------------------- | --------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `POST /email/send`               | auth only | `requirePermission('integrations:write')` | Outbound mail from the org mailbox is privileged — read-only users / read-scoped keys must not send (impersonation/abuse). |
| `POST /sms/send`                 | auth only | `requirePermission('integrations:write')` | SMS send incurs cost + abuse/impersonation risk.                                                                           |
| `POST /integrations/twilio/test` | auth only | `requirePermission('integrations:read')`  | Leaked Twilio account-SID suffix + from-number (integration config) to any authenticated user.                             |
| `PATCH /crm/widgets`             | auth only | `requirePermission('settings:write')`     | Writes the org-wide dashboard layout (orgId-scoped, not per-user) — any role could rewrite every user's dashboard.         |

Verification: `pnpm --filter @bidstack/api typecheck` → exit 0. No existing test exercised these endpoints (so no regression); a behavioral 403 test requires a harness enhancement (see §5).

---

## 4. AI-compute gaps closed in current WIP

| Endpoint                           | Was       | Now                                                                                              | Verification                                                                                  |
| ---------------------------------- | --------- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------- |
| `POST /ai-assistant/email-draft`   | auth only | human session + `contacts:read` for contact/general drafts; `opportunities:read` for deal drafts | `apps/api/src/routes/ai-compute-auth.integration.test.ts` rejects API keys before AI compute. |
| `POST /ai-assistant/deal-*`        | auth only | human session + `opportunities:read`                                                             | same focused test file + RBAC decorator unit test.                                            |
| `POST /ai-assistant/meeting-prep`  | auth only | human session + `activities:read`                                                                | typecheck + focused auth tests.                                                               |
| `POST /ai-assistant/enrich-*`      | auth only | human session + `contacts:read`                                                                  | typecheck + focused auth tests.                                                               |
| `POST /ai-assistant/account-*`     | auth only | human session + `accounts:read`                                                                  | typecheck + focused auth tests.                                                               |
| `POST /ai-assistant/*/feedback`    | auth only | human session; feedback is now scoped to the caller's own AI session instead of any org session  | typecheck + focused auth tests.                                                               |
| `POST /calls/:id/extract-insights` | auth only | human session + `activities:write`                                                               | focused auth test rejects exact `activities:write` API keys before the handler.               |
| `GET /calls/:id`                   | auth only | `activities:read` for transcript + signed recording detail                                       | focused auth test rejects non-`activities:read` API keys before the handler.                  |

---

## 5. Design risks & follow-ups

- **Closed this pass:** REST API keys are no longer resource-agnostic in production. `requirePermission` now accepts exact `PermissionKey` scopes and rejects broad `read`/`write` unless the legacy migration flag is explicitly enabled. MCP transport keeps its separate `mcp` + `read`/`write` tool-class scopes.
- **Closed this pass:** AI compute routes now have behavioral 403 regression tests for API keys with exact REST scopes. The remaining harness follow-up is broader non-admin human simulation, not the API-key path.
- **Confirm:** every public webhook/OAuth endpoint verifies its signature/token/state (spot-checked: Twilio `X-Twilio-Signature`, Dust signature, OAuth `state` — all present).
