# Brand assets — marketing mirror

Same files as `apps/web/public/brand/`. Kept as a copy (not a symlink) because
the two apps deploy to different domains and have independent build pipelines.

See `apps/web/public/brand/README.md` for the slot table and spec.
