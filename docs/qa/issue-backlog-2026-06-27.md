# QA Issue Backlog — 2026-06-27

Source: 42 verified review findings across Opportunities, Leads, Accounts/KAM,
Forecasts/Dashboard, Proposals/RFP, Contacts/Tasks/Calendar, Settings/Analytics,
and Cross-sell/References/Sector surfaces.

**Dedup:** the two findings describing the Cross-sell KPI summary computed from
the status-filtered `items` array (`CrossSellPage.tsx:74-86`) were the same defect
reported once as `bug` and once as `ux`; merged into a single entry (#12).
**41 unique issues** remain.

**Ranking:** P1 bugs → P1 ux/a11y → remaining P2/P3, biased toward low-effort,
high-impact, data-correctness items first. No P0s were present in the set.

**Note on #1:** the compliance-matrix double-encode fix already appears applied in
the working tree (`useRfpCompliance.ts:49` now passes a plain object with a WHY
comment). Kept in the backlog as *verify the round-trip test exists, then close*.

Severity = reviewer's P1/P2/P3. Effort = S (≤½ day) / M (≤2 days) / L (>2 days).

| Rank | Title | Kind | Sev | Effort | File:Line | Fix |
|------|-------|------|-----|--------|-----------|-----|
| 1 | Compliance matrix answer save double-encodes request body (save broken) | bug | P1 | S | apps/web/src/hooks/rfp/useRfpCompliance.ts:43-50 | Pass `body: { answerDraft }` (not `JSON.stringify`) so `api()` encodes once. **Appears already fixed in tree** — add/confirm a round-trip test, then close. |
| 2 | Reorder then delete/add widget shows stale list (deleted widget persists, added one missing) | bug | P1 | M | apps/web/src/pages/AnalyticsDashboardPage.tsx:46-47,60-67 | Reconcile `localOrder` with `rawWidgets` in a `useEffect` keyed on `rawWidgets` identity (keep drag order for survivors, append new, drop removed); or `setLocalOrder([])` in add/delete `onSuccess`. |
| 3 | Clicking an existing calendar event opens the "new event" modal instead of viewing/editing | ux | P1 | M | apps/web/src/pages/CalendarPage.tsx:269-296 | Render each event as its own focusable button with `stopPropagation` `onClick` opening an event-detail/edit modal wired to the existing PATCH/DELETE `/calendar/events/:id` endpoints; keep the empty-slot create affordance separate. |
| 4 | Company required by API but not validated in New Lead form (blank → opaque 400) | bug | P2 | S | apps/web/src/pages/NewLeadPage.tsx:32-40,167-180 | Add Company to required-field validation (error flag, focus the input, `aria-invalid`, label `*`), mirroring the firstName/lastName pattern. |
| 5 | New Forecast dialog sends empty `ownerId`, server 400s (self-service default unreachable) | bug | P2 | S | apps/web/src/pages/forecasts/NewForecastDialog.tsx:44-47,90-101 | Omit `ownerId` from the payload when empty (`...(ownerId ? { ownerId } : {})`) or require a UUID in `canSubmit`. |
| 6 | CSV export fails (400) when a legacy-stage chip filter is active | bug | P2 | S | apps/web/src/pages/OpportunitiesPage.tsx:284-288 | Mirror the list query's branching: send `{ pipelineStageId }` for UUID stages, `{ stage }` for legacy enum stages. |
| 7 | Calendar events accept end time before start time (no client or server validation) | bug | P2 | S | apps/api/src/routes/calendar.ts:17-29 | Add `.refine(v => new Date(v.endAt) > new Date(v.startAt))` to the create schema (validate merged result on patch) and guard in `CreateEventModal.handleSubmit`. |
| 8 | GET /activities returns non-null `nextCursor` on the final page (phantom "next" page) | bug | P2 | S | apps/api/src/routes/activities.ts:158-161 | Emit a cursor only when `items.length === limit`; add an `(occurredAt, id)` tiebreaker to avoid dropping rows sharing a timestamp. |
| 9 | Reference tag filter options derived from the already-filtered list (can't switch tags) | bug | P2 | S | apps/web/src/pages/ReferencesPage.tsx:74,135-147 | Derive tag options from an unfiltered source (dedicated tags endpoint or a no-filter `useReferences()`), like industry options come from `useAccountIndustries`. |
| 10 | Deep link with an unknown `?id` renders "No dashboards" even when dashboards exist | bug | P2 | S | apps/web/src/pages/AnalyticsDashboardPage.tsx:32-37,182-200 | Fall back to `dashboards[0]` when the requested id isn't found (`?? dashboards[0]`); optionally strip the stale id from the query string. |
| 11 | Sector view "Largest sector" tile actually shows the currently selected sector | bug | P2 | S | apps/web/src/pages/SectorViewPage.tsx:182-193,41-42 | Bind the tile to the max-`accountCount` sector computed from `view.data.sectors`, independent of selection; or relabel it "Selected sector". |
| 12 | Cross-sell KPI cards (Active/Overdue/Unassigned/Next-due) computed from the filtered slice → misleading zeros | bug | P2 | M | apps/web/src/pages/CrossSellPage.tsx:74-86,131-168 | Compute KPI summary from an unfiltered dataset (separate status-omitted query or server-side aggregate); scope the status filter to the table only. *(merged duplicate)* |
| 13 | Dashboard "opportunities stalled" signal actually counts closed (won+lost) deals | bug | P2 | M | apps/web/src/components/dashboard/OrgDashboard.tsx:275 | Compute stalled from a real staleness metric (open opps, no recent activity / past-due close) from the summary endpoint, or relabel the pill. |
| 14 | Cross-sell empty state ignores the active status filter (misleading onboarding copy) | ux | P2 | S | apps/web/src/pages/CrossSellPage.tsx:199-206 | When `status !== 'all'`, render a filter-aware empty state with a "Show all" action (`setStatus('all')`); keep the create-from-cockpit copy only for `status === 'all'`. |
| 15 | Top Accounts empty state ignores active search/industry filters (wrong copy + CTA) | ux | P2 | S | apps/web/src/pages/TopAccountsPage.tsx:301-314 | Branch on `(search \|\| industry)`: show "No accounts match your filters" + Reset action when filtering; keep the "no data" copy/Browse-companies CTA only when no filter is set. |
| 16 | Cross-sell create form: textarea + unit inputs have no accessible label (placeholder only) | a11y | P2 | S | apps/web/src/components/account-intel/CrossSellCard.tsx:229-252 | Add `aria-label` to the description textarea and requesting/assigned-unit inputs, mirroring the already-labeled select/date fields (WCAG 3.3.2 / 1.3.1). |
| 17 | Settings nav buttons don't expose the active tab to assistive tech (no `aria-current`) | a11y | P2 | S | apps/web/src/components/settings/SettingsLayout.tsx:177-192 | Add `aria-current={active === item.id ? 'page' : undefined}` to the nav button (WCAG 1.3.1 / 4.1.2 — state by color/weight alone). |
| 18 | Stage-filter subtitle renders a raw UUID for configured pipeline stages | ux | P2 | S | apps/web/src/pages/opportunities/OppToolbar.tsx:156-158 | Look up `stageFilter` in `stageOptions` and render the resolved name; fall back to `formatStage` only for legacy enum ids. |
| 19 | Convert-to-opportunity panel is not focus-managed (no focus move, no Escape, not scrolled in) | a11y | P2 | M | apps/web/src/pages/leadDetail/ConvertLeadDialog.tsx:40-46 | Move focus to the first field on open, add `role="dialog"`/`aria-modal`/`aria-labelledby`, Escape-to-close, scroll into view, and `min={0}` on the value input. |
| 20 | Proposal section edit silently lost when save fails; no mutation error states | ux | P2 | M | apps/web/src/pages/ProposalDetailPage.tsx:165-168 | Keep the editor open, close only on `onSuccess`; render `updateSection.error` / `draftSection.error` inline (`role="alert"`) as `ProposalsPage` does for create. |
| 21 | Companies column sort only reorders the current 50-row page, not the full dataset | ux | P2 | M | apps/web/src/pages/CompaniesPage.tsx:81-83 | Push sort to the server (`orderBy` keyed off sort state) so pagination respects it, or visually scope the sort affordance to "this page". |
| 22 | Custom-object create modal only collects "name", ignoring the object's defined fields | ux | P2 | M | apps/web/src/pages/CustomObjectListPage.tsx:39-59,262-279 | Render the create form dynamically from `def.fields` (respect required/type), or validate against required fields and route users to the detail editor. |
| 23 | Drag-to-reorder widgets is purely cosmetic and silently lost on refresh | ux | P2 | L | apps/web/src/pages/AnalyticsDashboardPage.tsx:227-233 | Persist order via a widget-order PATCH on reorder, or remove the grab-cursor/disable dragging until persistence exists (header comment falsely claims it persists). |
| 24 | CSV export mislabels Score range as 0-10000 (actual 0-100) | bug | P3 | S | apps/web/src/pages/LeadsPage.tsx:236 | Change the header label to "Score (0-100)" (or "Score") to match the schema and the bulk-export label. |
| 25 | GET /forecasts list omits `deletedAt` filter, can surface soft-deleted rows | bug | P3 | S | apps/api/src/routes/territories-forecast.ts:31-40 | Add `deletedAt: null` to the `findMany` where clause, matching the projection and other tenant tables. |
| 26 | "new leads this week" signal shows total lead count (no week window) | bug | P3 | S | apps/web/src/components/dashboard/OrgDashboard.tsx:276 | Add a `leadsThisWeek` field to the summary and pass that, or change copy to "{{count}} leads in funnel". |
| 27 | Detail-page stage dropdown desyncs `pipelineStageId` for orgs on custom pipelines | bug | P3 | M | apps/web/src/pages/OpportunityDetailPage.tsx:294-304 | Drive the select from the org's configured pipeline stages and route changes through `useStageMutation()`/`/:id/stage` (as Mark Won/Lost does) so both fields stay in sync. |
| 28 | Top-account auto ranking counts `closed_lost` value as "total pipeline" | bug | P3 | M | apps/api/src/routes/accounts.ts:275 | Add `AND o.stage <> 'closed_lost'` to the SUM (and matching filter in `fetchAccountStats` totals), or split out an `openPipeline` metric; relabel if lifetime value is intended. |
| 29 | Contact delete-undo recreates records with new IDs, silently drops custom field values | bug | P3 | M | apps/web/src/pages/ContactsPage.tsx:262-289 | Implement undo as a server-side restore (clear `deletedAt` keyed by original id) preserving custom fields and relationships, instead of recreating from the in-memory snapshot. |
| 30 | Reference write actions shown to read-only users → 403 toast | ux | P3 | S | apps/web/src/pages/ReferencesPage.tsx:98,242-262 | Gate New/Delete/Use with `useHasPermission('accounts:write')`, as `CrossSellPage` already does. |
| 31 | Dashboards-list delete uses native `window.confirm` instead of the app dialog | ux | P3 | S | apps/web/src/pages/DashboardsListPage.tsx:200-205 | Replace `window.confirm` with the themed `confirm({ title, description, destructive: true })` from `@/components/ui/ConfirmDialog`. |
| 32 | Advance-status button disables all rows during any single in-flight patch | ux | P3 | S | apps/web/src/pages/CrossSellPage.tsx:255 | Gate `disabled` on the per-row `busy` check (`patch.variables?.id === action.id`) instead of the shared `patch.isPending`; same in `CrossSellCard.tsx:129`. |
| 33 | Compliance row Save button shows global saving state across all open rows | ux | P3 | S | apps/web/src/components/rfp/compliance/ComplianceMatrix.tsx:81-86 | Pass saving state only to the active row (`saveRow.variables?.rowId === row.id`) and surface `saveRow.isError` per row. |
| 34 | Forecast cell blur fires a write even when the value is unchanged | ux | P3 | S | apps/web/src/pages/forecasts/ForecastList.tsx:71-81 | Compare the parsed value against `editingCell.value` and only call `onSaveCell` when it actually changed. |
| 35 | Tasks header miscounts "active follow-ups" (current-page length incl. done tasks) | ux | P3 | S | apps/web/src/pages/TasksPage.tsx:190-194 | Drive the subtitle from the task summary's `open` field (`useTaskSummary`) instead of `data.items.length`. |
| 36 | ERP sync failure surfaced twice (transient toast + persistent banner, two Retries) | ux | P3 | S | apps/web/src/pages/AccountsPage.tsx:210-317 | Pick one channel: keep the persistent inline banner+Retry and drop the `onError` toast (or vice versa). |
| 37 | Compliance status labels + confidence text hardcoded English, bypass i18n | a11y | P3 | S | apps/web/src/components/rfp/compliance/ComplianceRow.tsx:24-29,134-148 | Route `STATUS_LABELS`, "AI confidence:", "% confidence", and the status `aria-label` through `t()` with `rfp.json` keys. |
| 38 | Export button `aria-label` claims "visible" rows but export streams all matching rows | a11y | P3 | S | apps/web/src/pages/opportunities/OppToolbar.tsx:183 | Change the label/copy to "Export all matching opportunities as CSV" (respecting the active filter). |
| 39 | RfpResponseHub "Bid/No-Bid" button accessible name doesn't match visible label | a11y | P3 | S | apps/web/src/pages/RfpResponseHubPage.tsx:111-119 | Give the button an `aria-label` containing its visible text (or drop it); also decide whether this now-redirected dead-code page should ship (WCAG 2.5.3). |
| 40 | Leads column sort only reorders the current page while pagination is server-side | ux | P3 | M | apps/web/src/pages/LeadsPage.tsx:100-107,452-461 | Push sort key/dir to the server query for global ordering, or visually scope the sort to "this page" and surface that to the user. |
| 41 | Multi-hour and all-day calendar events render only in their start-hour cell | ux | P3 | M | apps/web/src/pages/CalendarPage.tsx:113-120 | Bucket events into every overlapped hour row (`start <= hour < end`) and render `isAllDay` events in a dedicated all-day lane above the grid. |
